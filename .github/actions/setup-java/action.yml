name: Setup Java
description: Sets up the JDK used to build SocialFood

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: 21
        cache: gradle